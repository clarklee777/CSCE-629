#include "heap.hpp"

int main()
{
    heap * the_heap;
    FILE *fp;
    fp = fopen("testgraph.txt", "r");
    
    //fscanf
    //printf("Not ready for testing yet.\n");
    
    fclose(fp);
    return 0;
}
